/**
 * Intent flags for Japan-specific autocomplete / lookup.
 * API providers are runtime-owned; schema only declares intent.
 */
export type YsPostalLookup = boolean;
export type YsAddressLookup = boolean;
export type YsCorporateNumberLookup = boolean;
